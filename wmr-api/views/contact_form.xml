<?xml version="1.0"?>
<odoo>
    <!-- 
    <record id="action_contacts_menu" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_models">res.partner.contacts</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="xml">
            Create A Contact
        </field>
    </record> -->

    <record id="action_contacts_menu" model="ir.actions.act_window">
        <field name="name">Sub Contacts</field>
        <field name="res_model">res.partner.contacts</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a Sub Contact
            </p>
        </field>
    </record>
</odoo>